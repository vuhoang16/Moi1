{
  "name": "rentapp",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @rentapp/api dev",
    "dev:mobile": "pnpm --filter @rentapp/mobile start",
    "build": "pnpm --filter @rentapp/api build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
